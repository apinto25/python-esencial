"""
Ejercicio 1: Try - Except

Escribe una función "dividir" que reciba dos números (a, b) y retorne
a / b.

1. Usa try/except para manejar el error cuando b es 0 (ZeroDivisionError),
   imprimiendo un mensaje amigable en vez de dejar que el programa falle.
2. Usa try/except para manejar el error cuando a o b no son números
   (TypeError), imprimiendo otro mensaje amigable.
3. Prueba la función con al menos tres casos: una división válida,
   una división por cero y una división con un valor que no sea número.
"""

# TODO: define la función dividir con manejo de excepciones

# TODO: prueba la función con los tres casos descritos
